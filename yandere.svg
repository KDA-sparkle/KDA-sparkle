name: Generate Glitch Text Animation

on:
  schedule:
    - cron: "0 */6 * * *"  # Met à jour toutes les 6 heures
  push:
  workflow_dispatch:

jobs:
  glitch-text:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Generate Glitch Text SVG
        run: |
          echo '<svg xmlns="http://www.w3.org/2000/svg" width="600" height="100">
            <text x="50%" y="50%" font-size="36" fill="red" text-anchor="middle" dominant-baseline="middle">
              You can’t escape me...
            </text>
            <text x="50%" y="70%" font-size="36" fill="white" text-anchor="middle" dominant-baseline="middle">
              I see everything...
            </text>
            <text x="50%" y="90%" font-size="36" fill="pink" text-anchor="middle" dominant-baseline="middle">
              Welcome to your nightmare...
            </text>
          </svg>' > glitch-text.svg

      - name: Commit and Push SVG
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"
          git add glitch-text.svg
          git commit -m "🔪 Updated glitch text animation"
          git push
