name: Generate GitHub Metrics

on:
  schedule:
    - cron: "0 */1 * * *"  # Exécute toutes les heures
  push:
    branches:
      - main  # Exécute aussi à chaque push sur la branche principale
  workflow_dispatch:  # Permet de l'exécuter manuellement via GitHub Actions

permissions:
  contents: write  # Donne à GitHub Actions le droit d'écrire dans le dépôt

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false  # Désactive les credentials GitHub Actions par défaut

      - name: Generate GitHub Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: github-metrics.svg
          token: ${{ secrets.GITHUB_TOKEN }}
          base: header, activity, community, repositories
          config_timezone: Europe/Paris

      - name: Commit and Push Metrics
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"
          git add github-metrics.svg || echo "No changes to commit"
          git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Updated GitHub metrics" && git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/KDA-sparkle/KDA-sparkle.git HEAD:main)
